import React from "react";

export const Footer = () => {
  return (
    <div
      style={{
        padding: 10,
        height: 50,
        backgroundColor: "#282b2a",
        color: "#fff",
        textAlign: "center",
        display: "flex",
        flexDirection: "row",
        justifyContent: "space-between",
        boxShadow: "3px 3px 3px gray"
      }}
    >
      <h3>Footer</h3>
    </div>
  );
};
